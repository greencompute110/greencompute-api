apiVersion: v1
kind: Service
metadata:
  name: greenference-control-plane
spec:
  selector:
    app: greenference-control-plane
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000

